apiVersion: v1
kind: Service
metadata:
  labels:
    app: postgres
  name: postgres
  namespace: api-orchestrator-staging
spec:
  ports:
  - port: 5432
    targetPort: 5432
  selector:
    app: postgres
